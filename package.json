{
  "name": "boards-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx concurrently \"npm run server\" \"npm run client\"",
    "server": "npx cross-env-shell \"cd server && npm start\"",
    "client": "npx cross-env-shell \"cd client && npm start\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "cross-env": "^7.0.3"
  }
}
